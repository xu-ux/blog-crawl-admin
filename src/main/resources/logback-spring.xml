<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 文件输出格式 -->
<!--    <property name="PATTERN" value="%-12(%d{yyyy-MM-dd HH:mm:ss.SSS}) |-%-5level [%thread] %c [%L] -| %msg%n" />-->

    <!-- 彩色日志格式 -->
    <!-- 彩色日志依赖的渲染类 -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />
    <conversionRule conversionWord="wEx" converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter" />
    <property name="PATTERN" value="${LOG.PATTERN:-%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG.LEVEL.PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(-){faint} %clr([%10.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG.EXCEPTION.CONVERSION_WORD:-%wEx}}"/>

    <!-- 非彩色日志格式 %d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度 %msg：日志消息，%n是换行符 -->
<!--    <property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%8.15thread] [%-42.42logger{16}] %4.4line : %m%n" />-->


    <property name="LOG_NAME" value="crawler" />
    <!--所在项目包 打印日志用-->
    <property name="PACKAGE_NAME" value="com.bs" />

    <!-- dev文件路径 -->
    <property name="LOG_PATH" value="logs"/>
    <property name="DEV_FILE_PATH" value="${LOG_PATH}/${LOG_NAME}-dev" />
    <!-- test文件路径 -->
    <property name="TEST_FILE_PATH" value="${LOG_PATH}/${LOG_NAME}-test" />
    <!-- pro文件路径 -->
    <property name="PROD_FILE_PATH" value="${LOG_PATH}/${LOG_NAME}-pro" />

    <!-- 开发环境 -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${PATTERN}</pattern>
            </encoder>
        </appender>

        <!-- 输出日志到文件：DEBUG -->
        <appender name="DEV_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${DEV_FILE_PATH}.log</file>
            <append>true</append>
            <encoder>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%8.15thread] [%-42.42logger{16}] %4.4line : %m%n</Pattern>
                <charset>UTF-8</charset>
            </encoder>

            <!-- 日志记录级别 级别过滤器 -->
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>DEBUG</level>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>ACCEPT</onMismatch>
            </filter>

            <!-- 时间滚动策略 -->
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <!-- 保留天数 -->
                <maxHistory>7</maxHistory>
                <fileNamePattern>${DEV_FILE_PATH}/debug.%d{yyyy-MM-dd}.log</fileNamePattern>
            </rollingPolicy>
        </appender>

        <!-- 输出到文件 -->
        <logger name="org.springframework.jdbc.core.JdbcTemplate" level="debug"/>
        <logger name="org.apache.ibatis.reflection" level="INFO"/>

        <logger name="${PACKAGE_NAME}" level="debug" />
        <root level="info">
           <appender-ref ref="CONSOLE" />
            <appender-ref ref="DEV_FILE" />
        </root>
    </springProfile>
    
    <!-- 测试环境 -->
    <springProfile name="test">
        <!-- 每天产生一个文件 -->
        <appender name="TEST_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${TEST_FILE_PATH}${file.separator}${LOG_NAME}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- rollover daily -->
                <fileNamePattern>${TEST_FILE_PATH}${file.separator}${LOG_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <!-- each file should be at most 100MB, keep 60 days worth of history, but at most 20GB -->
                <maxHistory>7</maxHistory>
                <maxFileSize>100MB</maxFileSize>
                <totalSizeCap>20GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>
                    [%date{yyyy-MM-dd HH:mm:ss.SSS}] %X{logthreadId} %-5level %logger{80} %method  %line - %msg%n
                </pattern>
            </encoder>
        </appender>
        <logger name="org.springframework.jdbc.core.JdbcTemplate" level="debug"/>
        <logger name="org.apache.ibatis.reflection" level="INFO"/>

        <logger name="${PACKAGE_NAME}" level="debug" />
        <root level="debug">
            <appender-ref ref="TEST_FILE" />
        </root>
    </springProfile>
    
    <!-- 生产环境 -->
    <springProfile name="prod">
        <appender name="PROD_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${PROD_FILE_PATH}${file.separator}${LOG_NAME}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- rollover daily -->
                <fileNamePattern>${PROD_FILE_PATH}${file.separator}${LOG_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <!-- each file should be at most 100MB, keep 60 days worth of history, but at most 20GB -->
                <maxHistory>7</maxHistory>
                <maxFileSize>100MB</maxFileSize>
                <totalSizeCap>20GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>
                    [%date{yyyy-MM-dd HH:mm:ss.SSS}] %X{logthreadId} %-5level %logger{80} %method  %line - %msg%n
                </pattern>
            </encoder>
        </appender>
        <root level="warn">
            <appender-ref ref="PROD_FILE" />
        </root>
    </springProfile>
</configuration>